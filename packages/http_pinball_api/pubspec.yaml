name: http_pinball_api
description: A Flutter implementation of the TodosApi that uses http.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  meta: ^1.3.0
  rxdart: ^0.27.2
  pinball_api:
    path: ../pinball_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.0.1
